# Dependencies for lecture12_exercise.py (CPU only, no GPU needed)
torch>=2.2
transformers>=4.41,<4.50
sentence-transformers>=3.1,<5
sentencepiece
protobuf
# Intel-mac / Rosetta Python is limited to torch 2.2.x, which needs NumPy 1.x
numpy<2 ; sys_platform == "darwin" and platform_machine == "x86_64"
